.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install freeport

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv freeport
    $ pip install freeport
